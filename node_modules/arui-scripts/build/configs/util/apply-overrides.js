"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const app_configs_1 = __importDefault(require("../app-configs"));
let overrides = [];
overrides = app_configs_1.default.overridesPath.map(path => {
    try {
        return require(path);
    }
    catch (e) {
        return {};
    }
});
/**
 *
 * @param {String|String[]} overridesKey Ключи оверрайда
 * @param {Object} config Конфиг, к которому нужно применить оверрайды
 * @returns {*}
 */
function applyOverrides(overridesKey, config) {
    if (typeof overridesKey === 'string') {
        overridesKey = [overridesKey];
    }
    overridesKey.forEach(key => {
        overrides.forEach((override) => {
            if (override.hasOwnProperty(key)) {
                if (typeof override[key] !== 'function') {
                    console.error(`Override ${key} must be a function`);
                }
                config = override[key](config, app_configs_1.default);
            }
        });
    });
    return config;
}
exports.default = applyOverrides;
